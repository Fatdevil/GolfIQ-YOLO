# Events

When a round payload omits a net total, the leaderboard derives it as `gross − round(fullHcp × holes/18)` using the event hole range when available (otherwise the round hole span, capped to 18).
